<?xml version="1.0" encoding="UTF-8" ?>
<class name="TerrainConfiguration" inherits="Resource" api_type="extension" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		Every tunable parameter of a [Terrain3D], in one resource.
	</brief_description>
	<description>
		Holds the shape, look and collision settings for a [Terrain3D]. Nothing is generated until a configuration is assigned to [member Terrain3D.configuration].
		Every setter emits [signal Resource.changed], and the terrain node rebuilds in response, so edits apply live, in the editor as well as in a running game. The nested layers relay their own changes too: editing a [TerrainBiomeLayer] inside [member biome_layers], or the [member rock_layer], rebuilds the terrain just the same.
		Because it is a plain [Resource], a configuration can be saved as a [code].tres[/code] preset and shared between scenes, or swapped at runtime to move a terrain between looks.
		Properties are grouped in the inspector as [b]Terrain[/b], [b]Noise[/b], [b]Physics[/b], [b]Biome[/b], [b]Parallax[/b] and [b]Triplanar[/b].
	</description>
	<tutorials>
	</tutorials>
	<members>
		<member name="biome_layers" type="TerrainBiomeLayer[]" setter="set_biome_layers" getter="get_biome_layers" default="[]">
			The [TerrainBiomeLayer] resources the terrain picks between, by temperature and moisture. An empty array renders untextured terrain.
			At most [code]32[/code] layers are used; a longer array is truncated with a warning. Layers relay their own [signal Resource.changed], so editing one rebuilds the terrain.
		</member>
		<member name="clipmap_levels" type="int" setter="set_clipmap_levels" getter="get_clipmap_levels" default="6">
			Number of nested clipmap rings. Each additional level doubles the terrain's visible radius for a nearly fixed vertex cost, so this is the cheap way to see further, unlike [member mesh_resolution].
			Range [code]1[/code] to [code]10[/code].
		</member>
		<member name="height_scale" type="float" setter="set_height_scale" getter="get_height_scale" default="10.0">
			Vertical amplitude of the terrain, in world units: peaks reach roughly this far above and below [code]y = 0[/code]. Raising it makes slopes steeper everywhere, which also raises the slope values that [member rock_layer] is compared against.
			Range [code]0.1[/code] to [code]100.0[/code].
		</member>
		<member name="mesh_resolution" type="int" setter="set_mesh_resolution" getter="get_mesh_resolution" default="256">
			Number of quads along one side of a single clipmap block. Higher values mean finer geometry at every level, at a roughly quadratic cost in vertices.
			Range [code]1[/code] to [code]512[/code]. Values of [code]0[/code] or less are modified to [code]64[/code].
		</member>
		<member name="moisture_frequency" type="float" setter="set_moisture_frequency" getter="get_moisture_frequency" default="0.0006">
			Frequency of the moisture noise field; effectively the inverse of how large a wet or dry region is. Unlike temperature, moisture does not depend on height at all.
			Range [code]0.0001[/code] to [code]0.01[/code].
		</member>
		<member name="moisture_offset" type="Vector2" setter="set_moisture_offset" getter="get_moisture_offset" default="Vector2(-4000, 9000)">
			World-space offset applied when sampling the moisture noise field. Because temperature and moisture use separate offsets, changing this re-rolls which parts of a temperature band are wet or dry.
		</member>
		<member name="noise_base_frequency" type="float" setter="set_noise_base_frequency" getter="get_noise_base_frequency" default="0.002">
			Frequency of the first noise octave; effectively the inverse of feature size. Lower values give broad, rolling landforms, higher values give tight, busy ones.
			Range [code]0.0001[/code] to [code]0.1[/code].
		</member>
		<member name="noise_gain" type="float" setter="set_noise_gain" getter="get_noise_gain" default="0.5">
			Amplitude multiplier between consecutive octaves. Values below [code]0.5[/code] give smooth terrain dominated by the base octave; higher values let fine octaves through and produce rough, craggy ground.
			Range [code]0.0[/code] to [code]1.0[/code].
		</member>
		<member name="noise_lacunarity" type="float" setter="set_noise_lacunarity" getter="get_noise_lacunarity" default="2.0">
			Frequency multiplier between consecutive octaves. The standard value of [code]2.0[/code] doubles the frequency each octave; higher values spread the octaves further apart and make detail look less self-similar.
			Range [code]1.0[/code] to [code]5.0[/code].
		</member>
		<member name="noise_octaves" type="int" setter="set_noise_octaves" getter="get_noise_octaves" default="5">
			Number of fBm octaves summed to produce the terrain height. More octaves add finer detail, at a linear cost in both the shader and the CPU-side sampling that backs collision and [method Terrain3D.get_height_at].
			Range [code]1[/code] to [code]10[/code].
		</member>
		<member name="noise_preview" type="ImageTexture" setter="" getter="get_noise_preview">
			Read-only greyscale preview of the current noise settings, regenerated whenever one of the [code]noise_*[/code] properties changes. It exists for the inspector; it is not the heightmap the terrain is built from and has no setter.
		</member>
		<member name="physics_collision_layer" type="int" setter="set_physics_collision_layer" getter="get_physics_collision_layer" default="1">
			Physics layers the terrain's collision body belongs to. See [url=https://docs.godotengine.org/en/stable/tutorials/physics/physics_introduction.html#collision-layers-and-masks]Collision layers and masks[/url].
		</member>
		<member name="physics_collision_mask" type="int" setter="set_physics_collision_mask" getter="get_physics_collision_mask" default="0">
			Physics layers the terrain's collision body scans. The terrain body is static and does not need to detect anything to be collided [i]with[/i], so the default of [code]0[/code] is usually right.
		</member>
		<member name="physics_range" type="float" setter="set_physics_range" getter="get_physics_range" default="64.0">
			Side length, in world units, of the square collision area kept around the focus point. Larger values push the collision regrid further into the background but make each regrid more expensive, since [member mesh_resolution] samples are spread over more ground.
			Range [code]8.0[/code] to [code]512.0[/code].
		</member>
		<member name="pom_fade_end" type="float" setter="set_pom_fade_end" getter="get_pom_fade_end" default="120.0">
			View distance, in world units, at which parallax occlusion mapping is fully faded out and costs nothing. Keep it comfortably above [member pom_fade_start] so the fade is not visible as a band, and low enough that distant terrain does not pay for detail nobody can see.
		</member>
		<member name="pom_fade_start" type="float" setter="set_pom_fade_start" getter="get_pom_fade_start" default="40.0">
			View distance, in world units, at which parallax occlusion mapping begins fading out. Closer than this it is applied at full strength.
		</member>
		<member name="pom_max_steps" type="int" setter="set_pom_max_steps" getter="get_pom_max_steps" default="32">
			Highest number of ray-marching steps parallax occlusion mapping uses, taken at grazing angles where the ray travels furthest through the height field. This is the main cost knob for parallax; lower it first if POM is too expensive.
			Range [code]1[/code] to [code]128[/code].
		</member>
		<member name="pom_min_steps" type="int" setter="set_pom_min_steps" getter="get_pom_min_steps" default="8">
			Lowest number of ray-marching steps parallax occlusion mapping uses, taken when a surface is viewed head-on and needs the least work.
			Range [code]1[/code] to [code]64[/code]. Only has an effect on layers whose [member TerrainBiomeLayer.pom_depth] is above zero.
		</member>
		<member name="rock_layer" type="TerrainSlopeLayer" setter="set_rock_layer" getter="get_rock_layer">
			Optional [TerrainSlopeLayer] painted over the biomes wherever the ground is steep enough, regardless of climate. Typically, a rock or cliff material.
			If it never appears, its [member TerrainSlopeLayer.slope_threshold] is above the steepest slope the terrain actually produces, see that class for how to pick a value.
		</member>
		<member name="temperature_altitude_reference" type="float" setter="set_temperature_altitude_reference" getter="get_temperature_altitude_reference" default="10.0">
			Height, in world units, over which temperature falls across its full range. Lower values compress the whole climate gradient into a shorter climb, so biomes stack up in thinner altitude bands.
			This is what makes temperature behave as altitude in practice: with the default settings, a biome's temperature window selects an [i]elevation[/i] band, not a region of the map.
			Range [code]0.1[/code] to [code]100.0[/code].
		</member>
		<member name="temperature_frequency" type="float" setter="set_temperature_frequency" getter="get_temperature_frequency" default="0.0004">
			Frequency of the noise field mixed into the climate temperature. Only matters to the extent that [member temperature_noise_influence] is above zero.
			Range [code]0.0001[/code] to [code]0.01[/code].
		</member>
		<member name="temperature_noise_influence" type="float" setter="set_temperature_noise_influence" getter="get_temperature_noise_influence" default="0.4">
			How much noise perturbs the altitude-derived temperature, from [code]0.0[/code] (temperature is purely a function of height) to [code]1.0[/code] (noise dominates).
			Keep this low for tidy altitude-banded biomes, and raise it to break the bands up into patches.
			Range [code]0.0[/code] to [code]1.0[/code].
		</member>
		<member name="temperature_offset" type="Vector2" setter="set_temperature_offset" getter="get_temperature_offset" default="Vector2(10000, -6000)">
			World-space offset applied when sampling the temperature noise field. Change it to shift which climates land where without touching the terrain's shape.
		</member>
		<member name="terrain_size" type="float" setter="set_terrain_size" getter="get_terrain_size" default="256.0">
			World-space side length of the finest clipmap level, in world units. Each coarser level covers twice as much ground, so the total radius the terrain reaches is this multiplied by [code]2[/code] to the power of [member clipmap_levels].
			Range [code]1.0[/code] to [code]10000.0[/code].
		</member>
		<member name="triplanar_sharpness" type="float" setter="set_triplanar_sharpness" getter="get_triplanar_sharpness" default="4.0">
			Exponent controlling how sharply triplanar texture projection blends between the three axes. Low values give a wide, soft blend that can look washed out on flat ground; high values give a crisp transition that can show a visible seam on slopes near 45 degrees.
			Range [code]1.0[/code] to [code]16.0[/code].
		</member>
	</members>
</class>
